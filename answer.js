var darray=[2,6,7];
var newArr=[];

function compare() {
    for(i=0, i<darray.length,i++)
        if (darray[i]<newArr[1]) {
            newArr.append(darray[i])
            return newArr
        }
}

compare();